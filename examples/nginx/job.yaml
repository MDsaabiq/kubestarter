apiVersion: batch/v1
kind: Job

metadata:
  name: demo-job

spec:
  completions: 1
  parallelism: 1

  template:
    spec:
      containers:
      - name: busybox
        image: busybox

        command: ["sh", "-c", "echo Hello Kubernetes && sleep 5"]

      restartPolicy: Never
